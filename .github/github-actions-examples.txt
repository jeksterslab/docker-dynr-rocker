# daily
on:
  schedule:
    - cron: "0 0 * * *"

# weekly
on:
  schedule:
    - cron: "0 2 ? * SUN"

# weekly
on:
  schedule:
    - cron: "0 0 * * 0"

# monthly
on:
  schedule:
    - cron: "0 0 1 * *"

